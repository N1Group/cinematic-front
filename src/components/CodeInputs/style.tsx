import styled from '@emotion/styled';

export const $CodeInputs = styled.div({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  gap: 10,
  input: {
    textAlign: 'center',
  },
});
